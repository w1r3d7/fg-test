import React from 'react';

const headerStyle = {
  marginBottom: 20,
  textAlign: 'center'
};

const Header = () => (
  <h1 style={headerStyle}>This is a list of employees with contacts of our company</h1>
);

export default Header;
